{
  "name": "PROJECT_NAME",
  "version": "0.1.0",
  "description": "",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "node --experimental-vm-modules ./node_modules/.bin/jest",
    "testw": "node --experimental-vm-modules ./node_modules/.bin/jest --watch",
    "coverage": "node --experimental-vm-modules ./node_modules/.bin/jest --coverage",
    "build": "echo \"Placeholder. Build command to be added.\" && exit 1",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "husky": "^7.0.1",
    "jest": "^27.0.6",
    "lint-staged": "^11.0.1",
    "prettier": "^2.3.2"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  }
}
