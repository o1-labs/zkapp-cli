{
  "name": "snapp-cli",
  "version": "0.1.0",
  "description": "CLI to create a SNAPP (Snark App) for Mina Protocol.",
  "bin": {
    "snapp": "./src/bin/index",
    "snap": "./src/bin/index",
    "snapp-cli": "./src/bin/index",
    "snap-cli": "./src/bin/index"
  },
  "scripts": {
    "prepare": "husky install",
    "postinstall": "node src/postinstall"
  },
  "keywords": [
    "mina",
    "snapps",
    "snapp",
    "snap"
  ],
  "license": "Apache-2.0",
  "dependencies": {
    "chalk": "^4.1.1",
    "degit": "^2.8.4",
    "envinfo": "^7.8.1",
    "ora": "^5.4.1",
    "shelljs": "^0.8.4",
    "yargs": "^17.0.1"
  },
  "devDependencies": {
    "husky": "^7.0.1",
    "lint-staged": "^11.0.1",
    "prettier": "^2.3.2"
  },
  "lint-staged": {
    "**/*": [
      "prettier --write --ignore-unknown"
    ]
  }
}
