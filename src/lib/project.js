// Create a new SNAPP project with recommended dir structure, Prettier config,
// testing lib, etc. Warn if already exists and do NOT overwrite.
module.exports = function (name) {
  console.log('Creating new project:', name);
};
